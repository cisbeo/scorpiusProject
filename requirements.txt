# Core Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic>=2.10.3       # Updated for mistralai compatibility
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.0
alembic==1.13.0
asyncpg==0.29.0        # PostgreSQL async driver
psycopg2-binary==2.9.9 # PostgreSQL sync driver (for migrations)
redis==5.0.0           # Cache and session storage

# Authentication & Security
python-jose[cryptography]==3.3.0  # JWT tokens
passlib[bcrypt]==1.7.4            # Password hashing
python-multipart==0.0.6           # Form data support
email-validator==2.1.0            # Email validation

# Document Processing
PyPDF2==3.0.0          # PDF extraction (fallback)
python-magic==0.4.27   # File type detection
pypdf==6.1.0           # PDF manipulation
pdfplumber==0.11.7     # Table extraction from PDFs

# Vector Database Support
pgvector==0.4.1        # PostgreSQL vector extension

# Utilities
python-dotenv==1.0.0   # Environment variables
structlog==24.1.0      # Structured logging
tenacity==8.2.0        # Retry logic

# AI/ML Libraries - Simplified for compatibility
mistralai              # Mistral AI SDK for requirements extraction (let pip resolve version)
httpx                  # HTTP client (let pip resolve compatible version)
numpy<2                # Numerical computing
scikit-learn>=1.3.0    # Machine learning

# Development & Testing (optional)
pytest==7.4.0
pytest-asyncio==0.21.0
pytest-cov==4.1.0
# RAG Dependencies (added for implementation plan)
# Note: pgvector already includes asyncpg support
prometheus-client==0.19.0  # Metrics export
pandas>=2.0.0  # Data analysis for dashboard
