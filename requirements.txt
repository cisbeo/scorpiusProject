# Core Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.0
alembic==1.13.0
asyncpg==0.29.0        # PostgreSQL async driver
psycopg2-binary==2.9.9 # PostgreSQL sync driver (for migrations)
redis==5.0.0           # Cache and session storage

# Authentication & Security
python-jose[cryptography]==3.3.0  # JWT tokens
passlib[bcrypt]==1.7.4            # Password hashing
python-multipart==0.0.6           # Form data support
email-validator==2.1.0            # Email validation

# Document Processing
PyPDF2==3.0.0          # PDF extraction (fallback)
python-magic==0.4.27   # File type detection
unstructured[pdf]==0.18.15  # Advanced document extraction
pypdf==6.1.0           # PDF manipulation
pdfplumber==0.11.7     # Table extraction from PDFs

# NLP & Machine Learning
spacy==3.7.0           # NLP framework
scikit-learn==1.7.2    # Machine learning (TF-IDF, etc.)
sentence-transformers==5.1.1  # Semantic embeddings
numpy<2                # Numerical computing (v1.x for compatibility)

# RAG & Vector Search
llama-index==0.9.48    # Document indexing and retrieval
llama-index-embeddings-huggingface==0.1.0  # HuggingFace embeddings for LlamaIndex

# Vector Database Support
pgvector==0.4.1      # PostgreSQL vector extension

# Utilities
python-dotenv==1.0.0   # Environment variables
structlog==24.1.0      # Structured logging
httpx==0.26.0          # HTTP client for testing
tenacity==8.2.0        # Retry logic

# Development & Testing (optional, can be in requirements-dev.txt)
pytest==7.4.0
pytest-asyncio==0.21.0
pytest-cov==4.1.0